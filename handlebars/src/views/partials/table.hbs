<table class="table">
  <thead>
    <tr>
      <th scope="col">Cod Prod</th>
      <th scope="col">Descripción</th>
      <th scope="col">Precio</th>
      <th scope="col">Imagen</th>
    </tr>
  </thead>
  <tbody>

    {{#each products}}
        <tr>
            <th scope="row">{{this.id}}</th>
            <td>{{this.title}}</td>
            <td>{{this.price}}</td>
            <td>
                <img src={{this.thumbnail}} alt="img" width="50">

            </td>
        </tr>
    {{/each}}


  </tbody>
</table>